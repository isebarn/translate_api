table book {
  url varchar
  name varchar
  saved boolean default=False
  location integer default=0
}}

table text {
  book [ref: > book.id]
  text varchar
  translation varchar
  index integer
}

table user {
  username varchar
  password varchar
  books [ref: < book.id]
}

